# 檢查紀錄

本次完成 174 項自動检查，全部通過。完整結果：qa/validation.json。

## 實際測過
- 網站1 HTML／2 CSS／1 JS；完整教材另有1份內嵌 HTML簡報。
- 保留來源選擇器、圖片與附件路徑、ID唯一性、JS語法，以及 theme.css 被部署流程收集。
- 320、390、768、1024、1440、1920寬度沒有水平溢出；延迟圖片實際解碼。
- 手機選單、Escape、details開關、照片放大與原圖一致、圖說一致、返回焦點、單一共用dialog、目前章節。
- 列印前展開details、列印後恢復；沒有JS仍可閱讀和展開原生details。
- 只留1件專案+2張生活照、移除選填區塊時，排版和原有圖片放大仍運作。
- 60頁簡報逐頁範圍、內嵌圖片、前後切換、同步填空、完整Prompt及其固定檔案規則。

## 測試方法與界線
容器瀏覽器禁止 file:// 導航。使用 Chromium 的 set_content，把相同HTML、CSS、JS與圖片讀入記憶體後渲染；本機相對路徑由標準函式庫檢查。
沒有聲稱實際執行檔案雙擊開啟、VS Code登入、Copilot免費帳號任務或GitHub遠端CI/CD。未修改老師的遠端repo。

## 人工檢视
已檢视首頁、作品與對應簡報。素材來源維持CREDITS.md，沒有新增真人資料、字型檔案或外部依賴。
