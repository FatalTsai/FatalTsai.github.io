# 老師先看｜簡單的是修改，設計與互動保留

## 本次的修改
不再以「程式只剩21行」或「只有一張普通HTML」當成功指標。維持學生在 HTML 裡看得見個人內容，但把側欄、排版、手機版、照片放大和選單等放在固定輔助檔。

網站檔案數：1 HTML、2 CSS、1 JS。完整教材另有1個自帶樣式/互動的 `course.html`，整包的實體 HTML/JS/CSS 仍各不超過2個。沒有打包字型、Bootstrap 或 jQuery。

## 發給學生
只發 website/。所有正文仍在 `index.html`。第一堂讓學生改 EDIT 01–05 和圖片；styles.css、theme.css、app.js、.github 不用動。
圖片放大會讀目前 img 的 src/alt/鄰近圖說，所以不增加一份圖片路徑作業。

## 簡報
course.html 仍是60頁，最後兩頁仍是極簡概論。所有網站框選與改前/改後圖已換成這版；第4頁清楚拆開「可改內容」與「固定輔助」。
黃色內嵌填空仍同步，五部分可一次複製成主Prompt。第一堂不必操作所有60頁。

## 最低完成
1張avatar（字母圖可）、2張生活、1張專案圖。名字、短自介、本人分工、真實連結。沒有其餘經歷就刪示例，網站仍有完整排版。

## 課前
請把新版 website/ 全部內容更新到老師 Template：包含 theme.css、app.js、styles.css 與已同步的 workflow。若仍用舊 workflow，不會收集 theme.css。
本次未改遠端repo；請用教室網路實走 VS Code/GitHub登入、Copilot、Commit、Actions、Pages。
老師帳號需確認設成 Template repository 與 Pages Source=GitHub Actions。不要把教師簡報和筆記傳进学生的公開網站。

## 40分鐘仍按原課堂路線
0–10分處理帳號與檔案，10–15分示範元素，15–27分填一次主Prompt，27–35分審閱/上傳，35–40分看發布與最後兩頁概論。
帳號、網路、用量、發布等待不是保證。卡住先交本機版，不要求臨時付費。

## 保留與保護
圖片都是既有教材素材/字母頭像。姓名、學校與經歷是虚構教學例。只採學生願意公開的本人資料，素材與來源署名保留。
